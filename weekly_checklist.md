# Python Core — еженедельный чеклист

Правило: отмечать пункт только если можешь сделать это **без ИИ и без подглядывания**.  
Формат: `[x]` — сделано, `[ ]` — нет.

---

## Неделя 1: Data Model + Функции + Замыкания

### Могу объяснить (теория)
- [ ] Чем отличается `is` от `==` и что такое интернирование int/str
- [ ] Порядок вызова: `__getattr__` vs `__getattribute__` — в чём разница и когда каждый срабатывает
- [ ] Почему переопределение `__eq__` без `__hash__` ломает использование в set/dict
- [ ] Что такое `__slots__`, как влияет на память и почему несовместим с `__dict__`
- [ ] Что такое cell object в замыкании и как работает `nonlocal`
- [ ] Ловушка с `lambda` в цикле — почему все lambdas возвращают последнее `i`
- [ ] Разница `*args` / `**kwargs` / keyword-only / positional-only параметров
- [ ] `functools.lru_cache` — как кэширует, почему аргументы должны быть hashable

### Могу написать (практика)
- [ ] `Vector` с `__add__`, `__mul__`, `__abs__`, `__repr__`, `__eq__`, `__hash__`
- [ ] `lru_cache` вручную через `dict` с ограничением размера (LRU eviction)
- [ ] Функцию, которая возвращает функцию с корректным замыканием (не ловушка цикла)
- [ ] `partial` аналог через замыкание без `functools`

---

## Неделя 2: Декораторы + Дескрипторы + ООП/MRO

### Могу объяснить
- [ ] Порядок применения декораторов (bottom-up) vs порядок вызова (top-down)
- [ ] Зачем `functools.wraps` и что именно он копирует
- [ ] Разница data descriptor vs non-data descriptor и порядок поиска атрибута
- [ ] Как `property`, `classmethod`, `staticmethod` реализованы через дескрипторы
- [ ] MRO: нарисовать порядок вызовов для diamond-иерархии A→B, A→C, B→D, C→D
- [ ] Разница `type.__new__` vs `type.__init__`, как Python создаёт класс
- [ ] Когда нужен метакласс, а когда хватит `__init_subclass__`

### Могу написать
- [ ] Декоратор `retry(n, exceptions)` с аргументами (factory pattern)
- [ ] Class-based декоратор с сохранением состояния между вызовами
- [ ] Дескриптор `ValidatedAttribute` — проверяет тип и диапазон при set
- [ ] `property` полностью через дескриптор вручную (без `@property`)
- [ ] Метакласс-реестр: автоматически регистрирует все подклассы

---

## Неделя 3: Итераторы + Генераторы + Контекстные менеджеры

### Могу объяснить
- [ ] Разница: iterable vs iterator (два протокола, два метода)
- [ ] Что происходит внутри `yield from` с вложенным генератором
- [ ] Как работает `gen.send(value)` и `gen.throw(exc)` — что происходит со стеком
- [ ] Когда `__exit__` подавляет исключение и каким образом (return True)
- [ ] Почему генератор исчерпывается молча (vs Java Stream)
- [ ] `ExitStack` — для чего и когда лучше, чем вложенные `with`

### Могу написать
- [ ] Бесконечный генератор Фибоначчи через `yield`
- [ ] Обход дерева через `yield from` (рекурсивный генератор)
- [ ] Контекстный менеджер для транзакции БД через `@contextmanager`
- [ ] `ExitStack` для динамического числа ресурсов
- [ ] Конвейер из `itertools.chain` + `islice` + `groupby`

---

## Неделя 4: Typing + Память + Import System

### Могу объяснить
- [ ] `Protocol` vs ABC — в чём разница, structural vs nominal subtyping
- [ ] `TypeVar` vs `ParamSpec` — когда нужен каждый
- [ ] Как Python считает references (`sys.getrefcount`) и когда удаляет объект
- [ ] Cyclic garbage collector: три поколения, когда включается
- [ ] `weakref` — когда и зачем (паттерн: кэш с автоудалением)
- [ ] Механизм import: `sys.path`, finders, loaders, `sys.modules`
- [ ] Circular import — почему возникает и три способа починить

### Могу написать
- [ ] `Protocol` для duck typing + `runtime_checkable` + проверка `isinstance`
- [ ] Кэш через `WeakValueDictionary` (объекты удаляются когда нет других ссылок)
- [ ] Воспроизвести circular import и починить переносом `import` внутрь функции
- [ ] Профилировать утечку памяти через `tracemalloc`
- [ ] Generic-класс через `TypeVar` + `Generic[T]`

---

## Неделя 5: Конкурентность + asyncio

### Могу объяснить
- [ ] GIL: что именно защищает, когда освобождается (I/O, C-extensions, check interval)
- [ ] CPU-bound vs I/O-bound: почему threads не ускоряют CPU-bound
- [ ] `Lock` vs `RLock` — разница, когда RLock нужен
- [ ] Event loop: как coroutine приостанавливается и возобновляется
- [ ] `await` под капотом: `__await__`, `send(None)` к генератору
- [ ] `gather` vs `TaskGroup` — разница в обработке ошибок
- [ ] `asyncio.shield` — от чего защищает и когда нужен
- [ ] `run_in_executor` — зачем и как избежать блокировки event loop

### Могу написать
- [ ] Producer-consumer на `asyncio.Queue` с несколькими воркерами
- [ ] `ThreadPoolExecutor` для параллельных HTTP-запросов
- [ ] `ProcessPoolExecutor` для CPU-bound задачи (например, факториал)
- [ ] Отменить корутину через `Task.cancel()` и обработать `CancelledError`
- [ ] Семафор для ограничения concurrency в async-коде

---

## Неделя 6: Стандартная библиотека + Производительность + Тестирование

### Могу объяснить
- [ ] `defaultdict` vs `Counter` vs `ChainMap` — когда что
- [ ] `deque` vs `list` — сложность append/appendleft/pop, когда предпочесть
- [ ] `logging` иерархия: logger → handler → formatter; `LoggerAdapter`
- [ ] `cProfile` вывод: cumtime vs tottime, как найти узкое место
- [ ] `Mock` vs `MagicMock` — разница (magic methods)
- [ ] `patch` vs `patch.object` — когда что патчить
- [ ] Branch coverage vs line coverage

### Могу написать
- [ ] Полный тест-сьют с `pytest`: fixtures (session/function scope), parametrize, conftest
- [ ] `patch` на внешний API-вызов внутри функции с `side_effect`
- [ ] `MagicMock` с `spec=` (защита от несуществующих атрибутов)
- [ ] Custom `logging.Handler` (например, отправляет в in-memory buffer)
- [ ] Профилировать мини-скрипт и заменить медленный участок на более быстрый вариант

---

## Неделя 7–8: Mock-собес + Итоговая проверка

### Senior-вопросы (ответить вслух, 2–3 минуты каждый)
- [ ] Объясни GIL: что, почему, когда мешает, когда не мешает
- [ ] Что такое descriptor protocol и как его использует `property`
- [ ] MRO + super() при множественном наследовании — нарисуй на примере
- [ ] Чем `Protocol` лучше ABC при утином типизировании
- [ ] Как реализовать кэш с TTL на Python без внешних библиотек
- [ ] asyncio event loop — как работает под капотом
- [ ] `__new__` vs `__init__` — зачем каждый, пример когда нужен `__new__`
- [ ] Cyclic GC — когда простого refcount недостаточно
- [ ] Замыкание в цикле — ловушка и три способа починить
- [ ] `weakref` — почему не просто слабый указатель, что происходит когда объект умирает

### Итоговый код-ревью (без ИИ)
- [ ] Найти один реальный open-source Python PR и объяснить каждое решение
- [ ] Написать `mini_di.py` — простой DI-контейнер (как в Java-части) на Python
- [ ] Code review своего кода двухнедельной давности — что бы изменил

---

## Прогресс по неделям

| Неделя | Дата начала | Теория | Практика | Статус |
|--------|-------------|--------|----------|--------|
| 1      |             | [ ]    | [ ]      |        |
| 2      |             | [ ]    | [ ]      |        |
| 3      |             | [ ]    | [ ]      |        |
| 4      |             | [ ]    | [ ]      |        |
| 5      |             | [ ]    | [ ]      |        |
| 6      |             | [ ]    | [ ]      |        |
| 7–8    |             | [ ]    | [ ]      |        |
